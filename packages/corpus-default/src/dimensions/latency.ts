import { defineDimension } from "@esbenwiberg/repofit/sdk";

export default defineDimension({
  id: "latency",
  name: "Latency",
  description: "How long does a verification cycle take?",
  gating: false,
});
