# acme-demo

Synthetic skill shipped by the acme overlay fixture.

This file is the canonical source for the acme-demo entry's only provide;
end-to-end tests copy it into a target repo via `agentry add`, then mutate
or remove it to exercise drift / upgrade / remove paths.
